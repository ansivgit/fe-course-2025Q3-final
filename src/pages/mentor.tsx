import { Title } from '@/components/title/title';

import type { ReactElement } from 'react';

export const Mentor = (): ReactElement => {
  return (
    <div>
      <Title>Mentor</Title>
    </div>
  );
};
